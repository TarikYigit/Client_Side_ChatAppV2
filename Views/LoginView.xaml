<UserControl x:Class="ClientSideChatApp.Views.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:helpers="clr-namespace:ClientSideChatApp.Helpers"
             d:DesignHeight="600" d:DesignWidth="800"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">

    <Grid>
        <!-- Sleek Gradient Background -->
        <Grid.Background>
            <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                <GradientStop Color="#0f2027" Offset="0.0"/>
                <GradientStop Color="#203a43" Offset="0.5"/>
                <GradientStop Color="#2c5364" Offset="1.0"/>
            </LinearGradientBrush>
        </Grid.Background>

        <!-- Floating Login Card -->
        <Border Width="350" Height="450" Background="#1E1E24" CornerRadius="15" HorizontalAlignment="Center" VerticalAlignment="Center">

            <!-- Smooth Drop Shadow -->
            <Border.Effect>
                <DropShadowEffect BlurRadius="25" ShadowDepth="8" Opacity="0.4" Color="Black"/>
            </Border.Effect>

            <StackPanel Margin="35">

                <!-- App Logo / Title -->
                <TextBlock Text="ChatApp" Foreground="White" FontSize="28" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,15,0,5"/>
                <TextBlock Text="Connect with your team" Foreground="#888888" FontSize="14" HorizontalAlignment="Center" Margin="0,0,0,35"/>

                <!-- Username Input -->
                <TextBlock Text="USERNAME" Foreground="#A0A0A0" FontSize="11" FontWeight="SemiBold" Margin="5,0,0,5"/>
                <Border Background="#2A2A35" CornerRadius="8" Padding="12,10" Margin="0,0,0,20">
                    <TextBox Text="{Binding UsernameInput, UpdateSourceTrigger=PropertyChanged}" 
                             Foreground="White" Background="Transparent" BorderThickness="0" 
                             CaretBrush="White" FontSize="15"/>
                </Border>

                <!-- Password Input (Masked!) -->
                <TextBlock Text="PASSWORD" Foreground="#A0A0A0" FontSize="11" FontWeight="SemiBold" Margin="5,0,0,5"/>
                <Border Background="#2A2A35" CornerRadius="8" Padding="12,10" Margin="0,0,0,30">
                    <PasswordBox helpers:PasswordBoxHelper.Attach="True"
                                 helpers:PasswordBoxHelper.BoundPassword="{Binding PasswordInput, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                 Foreground="White" Background="Transparent" BorderThickness="0" 
                                 CaretBrush="White" FontSize="15"/>
                </Border>

                <!-- Modern Login Button -->
                <Button Command="{Binding LoginCommand}" Margin="0,0,0,15" Cursor="Hand">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border x:Name="LoginBorder" Background="#0078D7" CornerRadius="8" Padding="0,12">
                                <TextBlock Text="LOG IN" Foreground="White" FontSize="15" FontWeight="Bold" HorizontalAlignment="Center"/>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="LoginBorder" Property="Background" Value="#005A9E"/>
                                </Trigger>
                                <Trigger Property="IsPressed" Value="True">
                                    <Setter TargetName="LoginBorder" Property="Background" Value="#004578"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Button.Template>
                </Button>

                <!-- Modern Register Button -->
                <Button Command="{Binding RegisterCommand}" Cursor="Hand">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border x:Name="RegBorder" Background="#28A745" CornerRadius="8" Padding="0,12">
                                <TextBlock Text="REGISTER" Foreground="White" FontSize="15" FontWeight="Bold" HorizontalAlignment="Center"/>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="RegBorder" Property="Background" Value="#218838"/>
                                </Trigger>
                                <Trigger Property="IsPressed" Value="True">
                                    <Setter TargetName="RegBorder" Property="Background" Value="#1E7E34"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Button.Template>
                </Button>

            </StackPanel>
        </Border>
    </Grid>
</UserControl>