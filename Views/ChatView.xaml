<UserControl x:Class="ClientSideChatApp.Views.ChatView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>
    </UserControl.Resources>

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Button Content="Back" Command="{Binding BackCommand}" Padding="10,5"/>
            <TextBlock Grid.Column="1" Text="{Binding HeaderText}" FontSize="18" FontWeight="Bold" VerticalAlignment="Center" Margin="15,0,0,0"/>
        </Grid>

        <ListBox Grid.Row="1" ItemsSource="{Binding Messages}">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel Margin="5">
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,2">
                            <TextBlock Text="{Binding Sender}" FontWeight="Bold" FontSize="12" Foreground="Gray"/>
                            <TextBlock Text="{Binding Timestamp}" FontSize="10" Foreground="DarkGray" Margin="10,0,0,0" VerticalAlignment="Bottom"/>
                        </StackPanel>

                        <TextBlock Text="{Binding Content}" FontSize="16" Margin="0,2,0,0"/>
                    </StackPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

        <TextBlock Grid.Row="2" Text="User is typing..." 
                   FontStyle="Italic" 
                   Foreground="Gray" 
                   Margin="5,2,0,5"
                   Visibility="{Binding IsTyping, Converter={StaticResource BoolToVis}}" />

        <Grid Grid.Row="3" Margin="0,10,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="80"/>
            </Grid.ColumnDefinitions>

            <TextBox Grid.Column="0" Text="{Binding InputText, UpdateSourceTrigger=PropertyChanged}" Padding="8" FontSize="16"/>

            <Button Grid.Column="1" Content="Send" Command="{Binding SendCommand}" Margin="5,0,0,0" 
                    Background="#0078D7" Foreground="White" BorderThickness="0" FontSize="16"/>
        </Grid>

    </Grid>
</UserControl>