<UserControl x:Class="ClientSideChatApp.Views.UserListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>

        <Style x:Key="DarkListBoxStyle" TargetType="ListBox">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="ItemContainerStyle">
                <Setter.Value>
                    <Style TargetType="ListBoxItem">
                        <!-- THE FIX: This forces the items to stretch so the badge snaps to the right side -->
                        <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="ListBoxItem">
                                    <Border x:Name="ItemBorder" Background="Transparent" CornerRadius="8" Padding="5" Margin="0,2">
                                        <ContentPresenter/>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsSelected" Value="True">
                                            <Setter TargetName="ItemBorder" Property="Background" Value="#3A86FF"/>
                                        </Trigger>
                                        <MultiTrigger>
                                            <MultiTrigger.Conditions>
                                                <Condition Property="IsMouseOver" Value="True"/>
                                                <Condition Property="IsSelected" Value="False"/>
                                            </MultiTrigger.Conditions>
                                            <MultiTrigger.Setters>
                                                <Setter TargetName="ItemBorder" Property="Background" Value="#2A2A35"/>
                                            </MultiTrigger.Setters>
                                        </MultiTrigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid>
        <!-- Gradient Background -->
        <Grid.Background>
            <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                <GradientStop Color="#0f2027" Offset="0.0"/>
                <GradientStop Color="#203a43" Offset="0.5"/>
                <GradientStop Color="#2c5364" Offset="1.0"/>
            </LinearGradientBrush>
        </Grid.Background>

        <Border Background="#1E1E24" CornerRadius="15" Margin="20">
            <Border.Effect>
                <DropShadowEffect BlurRadius="25" ShadowDepth="8" Opacity="0.4" Color="Black"/>
            </Border.Effect>

            <Grid Margin="20">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="20"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <!-- Search Bar -->
                <Grid Grid.Row="0" Grid.ColumnSpan="3" Margin="0,0,0,20">
                    <Border Background="#2A2A35" CornerRadius="8" Padding="12,10">
                        <TextBox x:Name="SearchBox" Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" 
                                 Foreground="White" Background="Transparent" BorderThickness="0" CaretBrush="White" FontSize="14" />
                    </Border>
                    <TextBlock IsHitTestVisible="False" Text="🔍 Search users or groups..." 
                               VerticalAlignment="Center" Margin="15,0,0,0" Foreground="#888888" FontSize="14">
                        <TextBlock.Style>
                            <Style TargetType="TextBlock">
                                <Setter Property="Visibility" Value="Collapsed"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding Text, ElementName=SearchBox}" Value="">
                                        <Setter Property="Visibility" Value="Visible"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </TextBlock.Style>
                    </TextBlock>
                </Grid>

                <!-- Headers -->
                <TextBlock Grid.Row="1" Grid.Column="0" Text="Users" Foreground="White" FontSize="18" FontWeight="Bold" Margin="5,0,0,10"/>
                <TextBlock Grid.Row="1" Grid.Column="2" Text="Groups" Foreground="White" FontSize="18" FontWeight="Bold" Margin="5,0,0,10"/>

                <!-- User ListBox -->
                <ListBox Grid.Row="2" Grid.Column="0" Style="{StaticResource DarkListBoxStyle}" ItemsSource="{Binding Users}" SelectedItem="{Binding SelectedUser}" FontSize="15">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <!-- THE FIX: Replaced StackPanel with a Grid so the badge pushes to the right -->
                            <Grid Margin="5">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>

                                <StackPanel Grid.Column="0" Orientation="Horizontal">
                                    <Ellipse Width="10" Height="10" Margin="0,0,12,0" VerticalAlignment="Center">
                                        <Ellipse.Style>
                                            <Style TargetType="Ellipse">
                                                <Setter Property="Fill" Value="#FF4C4C"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding IsOnline}" Value="True">
                                                        <Setter Property="Fill" Value="#28A745"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Ellipse.Style>
                                    </Ellipse>
                                    <TextBlock Text="{Binding Username}" VerticalAlignment="Center"/>
                                </StackPanel>

                                <!-- THE FIX: The Unread Badge -->
                                <Border Grid.Column="1" Background="#FF4C4C" CornerRadius="10" MinWidth="20" Height="20" Padding="5,0" 
                                        Visibility="{Binding HasUnreadMessages, Converter={StaticResource BoolToVis}}" VerticalAlignment="Center">
                                    <TextBlock Text="{Binding UnreadCount}" Foreground="White" FontSize="11" FontWeight="Bold" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                            </Grid>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>

                <!-- Group ListBox -->
                <ListBox Grid.Row="2" Grid.Column="2" Style="{StaticResource DarkListBoxStyle}" ItemsSource="{Binding Groups}" SelectedItem="{Binding SelectedGroup}" FontSize="15">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <!-- THE FIX: Replaced StackPanel with a Grid here too -->
                            <Grid Margin="5">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>

                                <StackPanel Grid.Column="0" Orientation="Horizontal">
                                    <TextBlock Text="👥" Margin="0,0,10,0" VerticalAlignment="Center" Foreground="#888888"/>
                                    <TextBlock Text="{Binding GroupName}" VerticalAlignment="Center"/>
                                </StackPanel>

                                <!-- THE FIX: The Unread Badge -->
                                <Border Grid.Column="1" Background="#FF4C4C" CornerRadius="10" MinWidth="20" Height="20" Padding="5,0" 
                                        Visibility="{Binding HasUnreadMessages, Converter={StaticResource BoolToVis}}" VerticalAlignment="Center">
                                    <TextBlock Text="{Binding UnreadCount}" Foreground="White" FontSize="11" FontWeight="Bold" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                            </Grid>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>

                <!-- Action Buttons -->
                <Grid Grid.Row="3" Grid.ColumnSpan="3" Margin="0,20,0,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <Button Grid.Column="1" Command="{Binding OpenCreateGroupCommand}" Margin="0,0,10,0" Cursor="Hand">
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border x:Name="BtnBorder" Background="#0078D7" CornerRadius="8" Padding="20,10">
                                    <TextBlock Text="Create Group" Foreground="White" FontSize="14" FontWeight="Bold"/>
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter TargetName="BtnBorder" Property="Background" Value="#005A9E"/>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Button.Template>
                    </Button>

                    <Button Grid.Column="2" Command="{Binding ConnectCommand}" Cursor="Hand">
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border x:Name="BtnBorder" Background="#28A745" CornerRadius="8" Padding="30,10">
                                    <TextBlock Text="Connect" Foreground="White" FontSize="14" FontWeight="Bold"/>
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter TargetName="BtnBorder" Property="Background" Value="#218838"/>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Button.Template>
                    </Button>
                </Grid>
            </Grid>
        </Border>
    </Grid>
</UserControl>