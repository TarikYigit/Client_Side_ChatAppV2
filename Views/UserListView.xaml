<UserControl x:Class="ClientSideChatApp.Views.UserListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Grid Margin="20">

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="20"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Search Bar -->
        <Grid Grid.Row="0" Grid.ColumnSpan="3" Margin="0,0,0,20">
            <TextBox x:Name="SearchBox" Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" 
                     Padding="8" FontSize="14" HorizontalAlignment="Stretch" />
            <TextBlock IsHitTestVisible="False" Text="🔍 Search users or groups..." 
                       VerticalAlignment="Center" Margin="10,0,0,0" Foreground="Gray" FontSize="14">
                <TextBlock.Style>
                    <Style TargetType="TextBlock">
                        <Setter Property="Visibility" Value="Collapsed"/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding Text, ElementName=SearchBox}" Value="">
                                <Setter Property="Visibility" Value="Visible"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </TextBlock.Style>
            </TextBlock>
        </Grid>

        <!-- Headers -->
        <TextBlock Grid.Row="1" Grid.Column="0" Text="Users" FontSize="18" FontWeight="Bold" Margin="0,0,0,10"/>
        <TextBlock Grid.Row="1" Grid.Column="2" Text="Groups" FontSize="18" FontWeight="Bold" Margin="0,0,0,10"/>

        <!-- User ListBox -->
        <ListBox Grid.Row="2" Grid.Column="0" ItemsSource="{Binding Users}" SelectedItem="{Binding SelectedUser}" FontSize="16">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal" Margin="10">
                        <CheckBox IsChecked="{Binding IsSelected}" VerticalAlignment="Center" Margin="0,0,10,0"/>
                        <Ellipse Width="12" Height="12" Margin="0,0,10,0" VerticalAlignment="Center">
                            <Ellipse.Style>
                                <Style TargetType="Ellipse">
                                    <Setter Property="Fill" Value="#FF4C4C"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsOnline}" Value="True">
                                            <Setter Property="Fill" Value="#28A745"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Ellipse.Style>
                        </Ellipse>
                        <TextBlock Text="{Binding Username}" VerticalAlignment="Center"/>
                    </StackPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

        <!-- Group ListBox -->
        <ListBox Grid.Row="2" Grid.Column="2" ItemsSource="{Binding Groups}" SelectedItem="{Binding SelectedGroup}" FontSize="16">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal" Margin="10">
                        <TextBlock Text="👥" Margin="0,0,10,0" VerticalAlignment="Center"/>
                        <TextBlock Text="{Binding GroupName}" VerticalAlignment="Center"/>
                    </StackPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

        <!-- Action Buttons -->
        <Grid Grid.Row="3" Grid.ColumnSpan="3" Margin="0,20,0,0">
            <Grid.ColumnDefinitions>
                <!-- Pushes buttons to the right -->
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <Button Grid.Column="1" Content="Create Group" Command="{Binding OpenCreateGroupCommand}" 
                    Margin="0,0,10,0" Padding="15,10" FontSize="16" Background="#0078D7" Foreground="White" BorderThickness="0" Cursor="Hand"/>

            <Button Grid.Column="2" Content="Connect" Command="{Binding ConnectCommand}" 
                    Padding="30,10" FontSize="16" Background="#28A745" Foreground="White" BorderThickness="0" Cursor="Hand"/>
        </Grid>

    </Grid>
</UserControl>