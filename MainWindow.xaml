<Window x:Class="ClientSideChatApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="clr-namespace:ClientSideChatApp.ViewModels"
        xmlns:views="clr-namespace:ClientSideChatApp.Views"
        Title="Client Side Chat App" Height="650" Width="1000"
        WindowStyle="None" 
        AllowsTransparency="True" 
        Background="Transparent" 
        WindowStartupLocation="CenterScreen">

    <Window.DataContext>
        <vm:MainViewModel/>
    </Window.DataContext>

    <Window.Resources>
        <DataTemplate DataType="{x:Type vm:LoginViewModel}">
            <views:LoginView/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:UserListViewModel}">
            <views:UserListView/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:ChatViewModel}">
            <views:ChatView/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:GroupChatViewModel}">
            <views:GroupChatView />
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:CreateGroupViewModel}">
            <views:CreateGroupView />
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:AddUserToGroupViewModel}">
            <views:AddUserToGroupView />
        </DataTemplate>
    </Window.Resources>

    <!-- The Main App Container -->
    <Border Background="#0f2027" CornerRadius="12" BorderBrush="#3A3A45" BorderThickness="1" ClipToBounds="True">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="35"/>
                <!-- Title Bar Row -->
                <RowDefinition Height="*"/>
                <!-- App Content Row -->
            </Grid.RowDefinitions>

            <!-- Custom Dark Title Bar -->
            <Border Grid.Row="0" Background="#15151C" CornerRadius="11,11,0,0" MouseLeftButtonDown="TopBar_MouseDown">
                <Grid>

                    <!-- THE NEW NAME -->
                    <TextBlock Text="Bizmuth" Foreground="#888888" FontWeight="SemiBold" FontSize="13" VerticalAlignment="Center" Margin="15,0,0,0"/>
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">

                        <!-- Minimize Button -->
                        <Button Content="—" FontWeight="Bold" Width="45" Background="Transparent" Foreground="#A0A0A0" BorderThickness="0" Cursor="Hand" Click="MinimizeApp_Click">
                            <Button.Template>
                                <ControlTemplate TargetType="Button">
                                    <Border x:Name="BtnBorder" Background="Transparent">
                                        <!-- Margin pushes the dash down slightly so it looks centered -->
                                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" Margin="0,2,0,0"/>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter TargetName="BtnBorder" Property="Background" Value="#2A2A35"/>
                                            <Setter Property="Foreground" Value="White"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Button.Template>
                        </Button>

                        <!-- Maximize / Restore Button -->
                        <Button Content="🗖" FontSize="14" Width="45" Background="Transparent" Foreground="#A0A0A0" BorderThickness="0" Cursor="Hand" Click="MaximizeApp_Click">
                            <Button.Template>
                                <ControlTemplate TargetType="Button">
                                    <Border x:Name="BtnBorder" Background="Transparent">
                                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" Margin="0,-2,0,0"/>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter TargetName="BtnBorder" Property="Background" Value="#2A2A35"/>
                                            <Setter Property="Foreground" Value="White"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Button.Template>
                        </Button>

                        <!-- Close Button -->
                        <Button Content="✕" Width="45" Background="Transparent" Foreground="#A0A0A0" BorderThickness="0" Cursor="Hand" Click="CloseApp_Click">
                            <Button.Template>
                                <ControlTemplate TargetType="Button">
                                    <Border x:Name="BtnBorder" Background="Transparent" CornerRadius="0,11,0,0">
                                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter TargetName="BtnBorder" Property="Background" Value="#E81123"/>
                                            <Setter Property="Foreground" Value="White"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Button.Template>
                        </Button>

                    </StackPanel>
                </Grid>
            </Border>

            <!-- Your App Content -->
            <ContentControl Grid.Row="1" Content="{Binding CurrentView}"/>

        </Grid>
    </Border>
</Window>