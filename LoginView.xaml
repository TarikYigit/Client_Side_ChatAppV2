<UserControl x:Class="ClientSideChatApp.Views.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Width="250">
        <TextBlock Text="ChatApp" FontSize="20" FontWeight="Bold" TextAlignment="Center" Margin="0,0,0,20"/>

        <TextBlock Text="Username:" Margin="0,0,0,5"/>
        <TextBox Text="{Binding UsernameInput, UpdateSourceTrigger=PropertyChanged}" 
                 Padding="5" FontSize="16" Margin="0,0,0,15"/>

        <TextBlock Text="Password:" Margin="0,0,0,5"/>
        <TextBox Text="{Binding PasswordInput, UpdateSourceTrigger=PropertyChanged}" 
                 Padding="5" FontSize="16" Margin="0,0,0,20"/>

        <Button Content="Login" Command="{Binding LoginCommand}" 
                Padding="5" FontSize="16" Background="#0078D7" Foreground="White" BorderThickness="0" Margin="0,0,0,10"/>

        <Button Content="Register" Command="{Binding RegisterCommand}" 
                Padding="5" FontSize="16" Background="#28A745" Foreground="White" BorderThickness="0"/>
    </StackPanel>
</UserControl>