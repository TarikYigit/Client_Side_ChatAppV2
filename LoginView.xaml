<UserControl x:Class="ClientSideChatApp.Views.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Width="250">
        <TextBlock Text="Enter Your Name" FontSize="20" FontWeight="Bold" TextAlignment="Center" Margin="0,0,0,20"/>

        <TextBox Text="{Binding UsernameInput, UpdateSourceTrigger=PropertyChanged}" 
                 Padding="8" FontSize="16" Margin="0,0,0,15"/>

        <Button Content="Enter App" Command="{Binding LoginCommand}" 
                Padding="10" FontSize="16" Background="#0078D7" Foreground="White" BorderThickness="0" Click="Button_Click"/>
    </StackPanel>
</UserControl>